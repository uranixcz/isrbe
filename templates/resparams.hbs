{{#*inline "page"}}
<table>
    <tr><th>ID</th><th>Name</th><th>Value</th><th>Movable?</th></tr>
    {{#each .}}
        <tr>
            <td>{{id}}</td>
            <td>{{name}}</td>
            <td>
                {{#if value.Number}}
                    {{value.Number}}
                {{else}}
                    {{#if value.Text}}
                        {{value.Text}}
                    {{else}}
                        {{#if value.Resource}}
                            {{value.Resource}}
                        {{else}}
                            -
                        {{/if}}
                    {{/if}}
                {{/if}} {{unit}}
            </td>
            <td>{{movable}}</td>
        </tr>
    {{/each}}
</table>
{{/inline}}
{{> layout}}