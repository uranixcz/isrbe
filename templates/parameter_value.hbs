{{#*inline "page"}}
    <form action="{{#if res_parameter}}/resource/parameter/{{res_param_id}}/modifyvalue{{else}}/resource/parameter/{{res_param_id}}/addvalue{{/if}}" method="get" accept-charset="utf-8">
        <table>
            <tr>
                <td><label for="value">Value</label></td>
                <td><input type="text" name="value" id="value" value="{{value.val}}" required autofocus/></td>
            </tr>
            {{#if is_type_resource}}
            <tr>
                <td><label for="dependency">Dependency</label></td>
                <td>
                    <select name="dependency" id="dependency" required>
                        {{#if res_parameter}}<option value="{{res_parameter.id}}" selected>{{res_parameter.name}}</option>{{/if}}
                        {{#each resources}}
                            <option value="{{id}}">{{name}} {{unit}}</option>
                        {{/each}}
                    </select>
                </td>
            </tr>
            {{/if}}
            <tr>
                <td>
                </td>
                <td><input type="submit" value="Submit" /></td>
            </tr>
        </table>
    </form>
{{/inline}}
{{> layout}}