{{#*inline "page"}}
    <form action="/modifyreslocation" method="get" accept-charset="utf-8">
        <table>
            <tr><th colspan="2">Modify resource location</th></tr>
            <tr>
                <td><label for="amount">Amount</label></td>
                <td><input type="number" id="amount" name="amount" min="0" step="0.01" value="{{location.amount}}" required /></td>
            </tr>
            <tr>
                <td><label for="unit">Unit</label></td>
                <td>
                    <select name="unit" id="unit" disabled>
                        <option value="{{location.unit_id}}" selected>Unchanged ({{location.unit}})</option>
                        {{#each parameters}}
                            <option value="{{id}}">{{name}} ({{unit}})</option>
                        {{/each}}
                    </select>
                </td>
            </tr>
            <tr>
                <td><label for="radius">Radius</label></td>
                <td><input type="number" id="radius" name="radius" min="0" value="{{location.radius}}" disabled /></td>
            </tr>
            <tr>
                <td><label for="location">Location (fixme)</label></td>
                <td>
                    <select name="location" id="location" disabled>
                        {{#each coordinates}}
                            <option value="{{id}}">lat:{{lat}} lon:{{lon}}</option>
                        {{/each}}
                    </select>
                </td>
            </tr>
            <tr>
                <td><input type="hidden" name="id" value="{{location.id}}"></td>
                <td><input type="submit" value="Submit" /></td>
            </tr>
        </table>
    </form>
{{/inline}}
{{> layout}}